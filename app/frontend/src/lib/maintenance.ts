/**
 * Maintenance mode configuration.

 * Set MAINTENANCE_MODE to true to activate the maintenance page site-wide.
 * When disabled, the site operates normally and search engines are not blocked.
 */
export const MAINTENANCE_MODE = false;